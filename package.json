{
  "name": "gpic-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "GPIC Profitability Optimizer - Simplex LP Solver with GLPK.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && git add dist -f && git commit -m 'Deploy to GitHub Pages' && git push origin $(git rev-parse --abbrev-ref HEAD):gh-pages"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.12.7",
    "glpk.js": "^1.5.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
